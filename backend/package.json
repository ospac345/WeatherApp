{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "weather app using node.js, express, and react", 
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"cd backend && node index.js", 
    "heroku-postbuild": "cd frontend/weather-app/ && npm install --only=dev --no-shrinkwrap && npm run build"
  }, 
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^2.6.11"
  }
}
